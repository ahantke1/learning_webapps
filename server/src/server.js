const app = require("./index");

// Expose our server to port 3001 so it can listen for requests
// https://en.wikipedia.org/wiki/Port_(computer_networking) - to learn more about ports
app.listen('3001', () => { })